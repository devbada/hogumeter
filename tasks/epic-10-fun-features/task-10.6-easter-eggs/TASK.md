# TASK-10.6: 이스터에그

## Task 정보

| 항목 | 내용 |
|------|------|
| Task ID | TASK-10.6 |
| Epic | EPIC-10 (재미 요소) |
| 난이도 | ⭐⭐ |
| 상태 | 준비 |
| 생성일 | 2025-12-15 |

---

## 목표

특정 조건에서 숨겨진 재미 요소를 발동시켜 사용자에게 예상치 못한 즐거움 제공

---

## 이스터에그 목록

| ID | 조건 | 이벤트 | 이모지/효과 |
|----|------|--------|-------------|
| EE-001 | 속도 88km/h 3초 유지 | "백 투 더 퓨처!" | ⚡🚗 번개 효과 |
| EE-002 | 요금 12,345원 도달 | "연속 숫자!" | 🌈 무지개 효과 |
| EE-003 | 심야 00:00~00:01 출발 | "신데렐라 모드" | 🎃🏰 호박마차 |
| EE-004 | 거리 42.195km 도달 | "마라톤 완주!" | 🏃‍♂️🏅 축하 |
| EE-005 | 요금 4,444원 도달 | "행운의 숫자!" | 🍀 클로버 효과 |
| EE-006 | 속도 100km/h 이상 | "광속 호구!" | 🚀 로켓 효과 |
| EE-007 | 요금 10,000원 정확히 | "만원의 행복" | 💰 동전 효과 |

---

## 구현 계획

### 1. 새 파일 생성

```
HoguMeter/
├── Domain/
│   ├── Entities/
│   │   └── EasterEgg.swift          # 이스터에그 엔티티
│   └── Services/
│       └── EasterEggManager.swift   # 이스터에그 감지 및 관리
├── Presentation/
│   └── Views/
│       └── Components/
│           └── EasterEggOverlayView.swift  # 이스터에그 효과 표시
```

### 2. EasterEgg 엔티티

```swift
enum EasterEggType: String, CaseIterable {
    case backToTheFuture   // 88km/h
    case sequentialNumber  // 12,345원
    case cinderella        // 00:00 출발
    case marathon          // 42.195km
    case luckyNumber       // 4,444원
    case rocketSpeed       // 100km/h
    case perfectTenK       // 10,000원
}

struct EasterEgg: Identifiable {
    let id: EasterEggType
    let title: String
    let emoji: String
    let message: String
}
```

### 3. EasterEggManager 서비스

```swift
@Observable
class EasterEggManager {
    var triggeredEasterEgg: EasterEgg?

    // 속도 기반 체크 (88km/h 3초 유지)
    private var highSpeedStartTime: Date?

    func checkSpeed(_ speed: Double)
    func checkFare(_ fare: Int)
    func checkDistance(_ distance: Double)
    func checkStartTime(_ date: Date)
}
```

### 4. EasterEggOverlayView

```swift
struct EasterEggOverlayView: View {
    let easterEgg: EasterEgg?

    // 3초 후 자동 dismiss
    // 이모지 + 메시지 애니메이션
    // 햅틱 피드백
}
```

---

## 구현 순서

### Step 1: 엔티티 정의
- [ ] `EasterEgg.swift` 생성
- [ ] EasterEggType enum 정의
- [ ] EasterEgg struct 정의

### Step 2: Manager 구현
- [ ] `EasterEggManager.swift` 생성
- [ ] 각 조건별 체크 로직 구현
- [ ] 88km/h 3초 유지 로직 (타이머 사용)
- [ ] 중복 발동 방지 로직

### Step 3: UI 구현
- [ ] `EasterEggOverlayView.swift` 생성
- [ ] 이모지 애니메이션 효과
- [ ] 메시지 표시 애니메이션
- [ ] 3초 후 자동 dismiss

### Step 4: 통합
- [ ] MeterViewModel에 EasterEggManager 연결
- [ ] MainMeterView에 오버레이 추가
- [ ] 햅틱 피드백 추가

### Step 5: 테스트
- [ ] 시뮬레이터에서 각 조건 테스트
- [ ] 빌드 확인

---

## 세부 조건 로직

### EE-001: 백 투 더 퓨처 (88km/h)
```swift
// 속도가 88km/h 이상일 때 타이머 시작
// 3초 유지 시 발동
// 한 번 발동 후 속도가 85km/h 아래로 떨어지면 리셋
```

### EE-002: 연속 숫자 (12,345원)
```swift
// 요금이 12,345원에 도달하면 발동
// 정확히 12,345원일 때만 (12,346원이 되면 놓침)
```

### EE-003: 신데렐라 모드 (00:00 출발)
```swift
// 미터기 시작 시간이 00:00 ~ 00:01 사이면 발동
// 주행 시작 시 1회만 체크
```

### EE-004: 마라톤 (42.195km)
```swift
// 거리가 42.195km (42,195m) 도달 시 발동
// 42.190km ~ 42.200km 범위 체크
```

### EE-005: 행운의 숫자 (4,444원)
```swift
// 요금이 4,444원에 도달하면 발동
```

### EE-006: 광속 호구 (100km/h)
```swift
// 속도가 100km/h 이상 도달 시 즉시 발동
// 주행 당 1회만
```

### EE-007: 만원의 행복 (10,000원)
```swift
// 요금이 정확히 10,000원일 때 발동
```

---

## 참고

- 이스터에그는 주행 당 각 타입별 1회만 발동
- 여러 조건이 동시에 충족되면 우선순위 높은 것만 표시
- 효과 표시 중에는 다른 이스터에그 발동 대기

---

## 예상 결과물

1. 주행 중 특정 조건 충족 시 화면에 재미있는 효과 표시
2. 사용자가 우연히 발견하는 숨겨진 재미 요소
3. SNS 공유 욕구 자극 ("야 나 이거 떴어!")
