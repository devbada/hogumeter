# Epic 9: 면책 안내 마키 텍스트 (Disclaimer Marquee)

## Epic 정보

| 항목 | 내용 |
|------|------|
| Epic ID | EPIC-9 |
| Epic 명 | 면책 안내 마키 텍스트 |
| 우선순위 | P0 (Must) |
| 예상 기간 | Week 5 |
| 상태 | 대기 |
| 생성일 | 2025-12-15 |

---

## 목표

미터기 작동 중 화면 배경에 "이 앱은 엔터테인먼트용입니다"라는 면책 안내 문구를 오른쪽에서 왼쪽으로 흐르는 마키(Marquee) 텍스트로 표시하여, 이 앱이 **실제 유료 택시미터기가 아님**을 사용자와 주변인에게 명확히 알림.

---

## 배경 및 필요성

### 법적 고려사항
- 택시미터기는 법적으로 규제된 계량기로, 무허가 사용 시 법적 문제 발생 가능
- 본 앱이 실제 택시미터기로 오인되는 것을 방지해야 함
- 엔터테인먼트/참고 목적임을 명시적으로 표시 필요

### 사용자 경험
- 배경에 은은하게 흐르는 텍스트로 재미 요소 추가
- 면책 문구가 눈에 잘 띄되, 메인 UI를 방해하지 않는 수준 유지

---

## 화면 설계

### 1. 마키 텍스트 위치 및 스타일

```
┌─────────────────────────────────────────┐
│          [미터기 메인 화면]              │
├─────────────────────────────────────────┤
│                                         │
│           ┌─────────────┐               │
│           │    🐴       │               │
│           │   말 애니    │               │
│           └─────────────┘               │
│                                         │
│  ← ← ← "엔터테인먼트용 앱입니다" ← ← ←   │  ← 마키 텍스트 (배경)
│                                         │
│           ┌─────────────┐               │
│           │  12,400원   │               │
│           │   요금표시   │               │
│           └─────────────┘               │
│                                         │
│  ← ← ← "실제 택시요금이 아닙니다" ← ← ←  │  ← 마키 텍스트 (배경)
│                                         │
│         [  정지하기  ]                   │
│                                         │
└─────────────────────────────────────────┘
```

### 2. 마키 텍스트 동작

```
시작: 화면 오른쪽 밖
  ─────────────────────────────────────────────►
                    [화면 영역]
  ◄─────────────────────────────────────────────
종료: 화면 왼쪽 밖

  "이 앱의 미터기 정보는 엔터테인먼트용입니다"
       ────────────────────────────────────►
                      (흐르는 방향: 오른쪽 → 왼쪽)
```

### 3. 스타일 가이드

```
┌─────────────────────────────────────────┐
│                                         │
│   텍스트 색상: 연한 회색 (opacity 30-40%)│
│   폰트: 시스템 폰트, 12-14pt             │
│   간격: 각 텍스트 사이 충분한 여백        │
│   속도: 약 50-80pt/초 (조절 가능)        │
│   위치: 화면 중간~하단 배경              │
│                                         │
└─────────────────────────────────────────┘
```

---

## 기능 요구사항 (Functional Requirements)

### FR-9.1: 마키 텍스트 기본 구현

| 항목 | 내용 |
|------|------|
| ID | FR-9.1 |
| 제목 | 마키 텍스트 기본 구현 |
| 우선순위 | P0 |
| 설명 | 오른쪽에서 왼쪽으로 흐르는 텍스트 애니메이션 구현 |

**수락 기준:**
- [ ] 텍스트가 화면 오른쪽 밖에서 시작하여 왼쪽 밖으로 사라짐
- [ ] 무한 반복 애니메이션
- [ ] 부드러운 선형 이동 (linear animation)
- [ ] 텍스트 완전히 사라진 후 다시 오른쪽에서 시작

---

### FR-9.2: 면책 문구 내용

| 항목 | 내용 |
|------|------|
| ID | FR-9.2 |
| 제목 | 면책 문구 정의 |
| 우선순위 | P0 |
| 설명 | 표시할 면책 문구 내용 정의 및 다국어 지원 |

**표시 문구 (한국어):**
```
주 문구: "이 앱의 미터기 정보는 엔터테인먼트용입니다"
부 문구: "실제 택시요금이 아닙니다 - 참고용으로만 사용하세요"
```

**수락 기준:**
- [ ] 위 문구가 번갈아 또는 함께 표시됨
- [ ] 텍스트는 Constants 또는 Localizable에서 관리
- [ ] 추후 다국어 지원 용이한 구조

---

### FR-9.3: 표시 조건

| 항목 | 내용 |
|------|------|
| ID | FR-9.3 |
| 제목 | 마키 텍스트 표시 조건 |
| 우선순위 | P0 |
| 설명 | 미터기 작동 중에만 표시 |

**수락 기준:**
- [ ] 미터기 상태가 `running`일 때만 표시
- [ ] `idle` 또는 `stopped` 상태에서는 숨김
- [ ] 지도 화면에서도 표시 (선택)

---

### FR-9.4: 스타일 및 가시성

| 항목 | 내용 |
|------|------|
| ID | FR-9.4 |
| 제목 | 마키 텍스트 스타일링 |
| 우선순위 | P1 |
| 설명 | 텍스트가 보이되 UI를 방해하지 않는 스타일 적용 |

**수락 기준:**
- [ ] 텍스트 색상: 연한 회색 (opacity 30-40%)
- [ ] 폰트: 시스템 폰트, 12-14pt
- [ ] 배경 레이어에 위치 (zIndex 낮음)
- [ ] 다크모드 대응 (밝은 배경에서는 어두운 텍스트)

---

### FR-9.5: 다중 텍스트 라인 (선택)

| 항목 | 내용 |
|------|------|
| ID | FR-9.5 |
| 제목 | 다중 마키 텍스트 라인 |
| 우선순위 | P2 |
| 설명 | 여러 줄의 마키 텍스트가 다른 속도/위치로 흐름 |

**수락 기준:**
- [ ] 2-3개의 마키 텍스트 라인
- [ ] 각 라인이 다른 속도로 이동 (시차 효과)
- [ ] 화면 여러 위치에 배치 (상단, 중단, 하단)

---

## 비기능 요구사항 (Non-Functional Requirements)

### NFR-9.1: 성능

| 항목 | 목표 |
|------|------|
| 애니메이션 프레임 | 60fps 유지 |
| CPU 사용량 | < 5% 추가 |
| 메모리 사용량 | < 5MB 추가 |

### NFR-9.2: 접근성

| 항목 | 목표 |
|------|------|
| VoiceOver | 면책 문구 읽기 지원 |
| 색상 대비 | WCAG 2.1 AA 기준 충족 (배경과의 대비) |

---

## 기술 설계

### 사용 기술

| 기술 | 용도 |
|------|------|
| SwiftUI | 마키 텍스트 뷰 구현 |
| Animation | 무한 반복 선형 애니메이션 |
| GeometryReader | 화면 크기 기반 위치 계산 |
| @State / @Binding | 애니메이션 상태 관리 |

### 구현 방식

```swift
// 마키 텍스트 뷰 예시 구조
struct MarqueeText: View {
    let text: String
    let speed: Double  // pt/초

    @State private var offset: CGFloat = 0

    var body: some View {
        GeometryReader { geometry in
            Text(text)
                .offset(x: offset)
                .onAppear {
                    startAnimation(screenWidth: geometry.size.width)
                }
        }
    }

    private func startAnimation(screenWidth: CGFloat) {
        // 오른쪽 밖에서 시작
        offset = screenWidth

        // 왼쪽 밖으로 이동 (무한 반복)
        withAnimation(.linear(duration: calculateDuration()).repeatForever(autoreverses: false)) {
            offset = -textWidth
        }
    }
}
```

### 프로젝트 구조 (추가)

```
Presentation/
├── Views/
│   ├── Components/
│   │   └── MarqueeTextView.swift      # 마키 텍스트 컴포넌트
│   │
│   └── Main/
│       └── MainMeterView.swift        # 마키 텍스트 배경 추가
│
Core/
├── Constants/
│   └── DisclaimerText.swift           # 면책 문구 상수 (기존 파일에 추가)
```

---

## Task 분해

### Task 목록

| Task ID | 제목 | 우선순위 | 예상 시간 | 의존성 |
|---------|------|----------|----------|--------|
| TASK-9.1 | MarqueeTextView 컴포넌트 구현 | P0 | 2시간 | - |
| TASK-9.2 | 면책 문구 상수 정의 | P0 | 0.5시간 | - |
| TASK-9.3 | MainMeterView 연동 | P0 | 1시간 | TASK-9.1, 9.2 |
| TASK-9.4 | 스타일 조정 및 다크모드 대응 | P1 | 1시간 | TASK-9.3 |
| TASK-9.5 | 다중 라인 마키 텍스트 (선택) | P2 | 1.5시간 | TASK-9.3 |
| TASK-9.6 | 지도 화면 연동 (선택) | P2 | 0.5시간 | TASK-9.3 |

### Task 상세

```
TASK-9.1: MarqueeTextView 컴포넌트 구현
├── MarqueeTextView.swift 생성
├── 오른쪽→왼쪽 애니메이션 구현
├── 무한 반복 설정
├── 속도 조절 파라미터
└── 재사용 가능한 컴포넌트로 설계

TASK-9.2: 면책 문구 상수 정의
├── DisclaimerText.swift에 마키용 문구 추가
├── 한국어 문구 정의
└── 추후 다국어 확장 고려

TASK-9.3: MainMeterView 연동
├── 미터기 화면 배경에 MarqueeTextView 추가
├── 미터기 running 상태에서만 표시
├── zIndex로 배경 레이어 설정
└── 기존 UI와 겹침 없이 배치

TASK-9.4: 스타일 조정 및 다크모드 대응
├── 텍스트 opacity 조정 (30-40%)
├── 폰트 크기 조정 (12-14pt)
├── 다크모드에서 색상 대응
└── 가독성 vs 비간섭 밸런스

TASK-9.5: 다중 라인 마키 텍스트 (선택)
├── 2-3개 마키 텍스트 라인 추가
├── 각 라인 다른 속도 설정
├── 화면 여러 위치에 배치
└── 시차 효과로 자연스러운 흐름

TASK-9.6: 지도 화면 연동 (선택)
├── MapContainerView 배경에 마키 텍스트 추가
└── 지도 위 반투명 레이어로 표시
```

---

## 테스트 시나리오

### 시나리오 1: 마키 텍스트 표시

1. 앱 실행
2. "시작하기" 버튼 탭
3. 미터기 작동 시작
4. 배경에 면책 문구가 오른쪽에서 왼쪽으로 흐르기 시작
5. 텍스트가 화면 왼쪽으로 사라지면 다시 오른쪽에서 등장

### 시나리오 2: 미터기 정지 시 숨김

1. 미터기 작동 중 (마키 텍스트 표시됨)
2. "정지하기" 버튼 탭
3. 미터기 정지
4. 마키 텍스트 사라짐

### 시나리오 3: UI 비간섭 확인

1. 미터기 작동 중
2. 마키 텍스트가 배경에서 흐름
3. 요금, 속도 등 메인 UI가 정상 표시됨
4. 마키 텍스트가 메인 UI를 가리지 않음

### 시나리오 4: 다크모드 대응

1. 시스템 다크모드 활성화
2. 앱 실행 및 미터기 시작
3. 마키 텍스트가 다크모드에 맞는 색상으로 표시

---

## 완료 기준

- [ ] 마키 텍스트가 오른쪽에서 왼쪽으로 부드럽게 흐름
- [ ] 면책 문구가 명확히 표시됨
- [ ] 미터기 작동 중에만 표시됨
- [ ] 메인 UI를 방해하지 않는 적절한 투명도
- [ ] 다크모드 대응 완료
- [ ] 60fps 애니메이션 성능 유지

---

## 구현 노트

(개발 중 기록)

---

## 발견된 이슈

(개발 중 기록)

---

## 참고 자료

- [SwiftUI Animation](https://developer.apple.com/documentation/swiftui/animation)
- [GeometryReader](https://developer.apple.com/documentation/swiftui/geometryreader)
- Marquee/Ticker 텍스트 구현 패턴
- docs/ARCHITECTURE.md
- docs/PRD.md
